using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class GameLocationController : MonoBehaviour
{
    //public GameObject randomObject;
    //float latitude = 26.85520f;
    //float longitude = 80.92250f;
    //public Transform Map;
    //public static LocationService deviceLocation;
    //int randomX;
    // 26.855094, 80.922528
    // Start is called before the first frame update
    void Start()
    {
        //randomx = random.range(-5, 5);
        //randomobject = instantiate(randomobject, new vector3(randomx,map.position.y,map.position.z), quaternion.identity) as gameobject;
        // deviceLocation.Start();
    }

    // Update is called once per frame
    void Update()
    {
      
    }
}
